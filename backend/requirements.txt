# --- Bhumika: audio pipeline ---
ffmpeg-python==0.2.0
imageio-ffmpeg==0.5.1      # bundles a static ffmpeg.exe, no system install needed
assemblyai==0.35.1
python-dotenv==1.0.1
tqdm==4.66.5

# --- shared / database ---
sqlalchemy==2.0.35

# --- dev / tests ---
pytest>=8.3.0
httpx>=0.28.1

# --- AI analysis (app/services/analyze.py): Groq primary, Ollama last resort ---
groq>=1.6.0
openai>=1.0.0
# openai package is used as an OpenAI-compatible client for the Ollama tier.
# Ollama itself needs the Ollama app installed + running locally
# (https://ollama.com) with `ollama pull llama3.1:8b` — not a pip package.

# --- transcription fallback chain (step2_transcribe.py): AssemblyAI -> Groq Whisper -> local ---
faster-whisper>=1.2.1

# --- Sneha: API + ChromaDB (installed here so the venv is shared) ---
# NOTE: version floors bumped from the original hackathon brief because this
# machine runs Python 3.13, and chromadb==0.5.5 / sentence-transformers==3.1.1
# pull in numpy<2.0, which has no prebuilt wheel for cp313 (pip tries to
# compile numpy from source and fails without a C compiler). These floors
# are the first versions with cp313 wheels available.
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
chromadb>=1.0.0
sentence-transformers>=3.2.0
